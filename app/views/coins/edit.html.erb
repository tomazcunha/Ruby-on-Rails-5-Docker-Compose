<!-- <h1>Editing Coin</h1> -->
<h1><%= t('links.editing.coin') %></h1>

<%= render 'form', coin: @coin %>

<%= link_to t('links.showing.coin'), @coin, class:"btn btn-primary" %> |
<%= link_to t('links.back'), coins_path, class:"btn btn-primary" %>
